export default function PropertyEditPage({
  params,
}: {
  params: { id: string };
}) {
  return (
    <div>
      <h1>Property Edit Page - {params.id}</h1>
      {/* Will be implemented later */}
    </div>
  );
}
